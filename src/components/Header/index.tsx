import { AppTitle, SubTitle, Container } from './styles'

const Header = () => {
  return (
    <Container>
      <AppTitle>Dictionary</AppTitle>
      <SubTitle>Find meanings & definitions</SubTitle>
    </Container>
  )
}

export default Header
